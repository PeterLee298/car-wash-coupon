<view class="login-container">
  <view class="login-header">
    <text class="title">洗车券核销</text>
    <text class="subtitle">商家端</text>
  </view>

  <view class="login-form">
    <view class="form-item">
      <input class="input" type="number" placeholder="请输入手机号" maxlength="11" value="{{phone}}" bindinput="onPhoneInput" />
    </view>
    <view class="form-item code-item">
      <input class="input code-input" type="number" placeholder="请输入验证码" maxlength="6" value="{{code}}" bindinput="onCodeInput" />
      <view class="code-btn {{!canSendCode || countdown > 0 ? 'disabled' : ''}}" bindtap="sendCode">
        {{countdown > 0 ? countdown + 's' : '获取验证码'}}
      </view>
    </view>
  </view>

  <button class="login-btn {{loading ? 'loading' : ''}}" bindtap="login" disabled="{{loading}}">
    {{loading ? '登录中...' : '登录'}}
  </button>

  <view class="tips">
    <text class="tips-text">请使用已注册的商家手机号登录</text>
  </view>
</view>
